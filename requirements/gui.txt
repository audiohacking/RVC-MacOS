# PyTorch Installation for macOS
# SECURITY: Using torch >= 2.6.0 to address critical vulnerabilities:
#   - CVE: Heap buffer overflow (fixed in 2.2.0)
#   - CVE: Use-after-free (fixed in 2.2.0)
#   - CVE: torch.load RCE with weights_only=True (fixed in 2.6.0)
#
# For macOS (Apple Silicon M1/M2/M3 or Intel):
# Install torch with MPS (Metal Performance Shaders) support
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# Other dependencies
einops
fairseq
flask
flask_cors
gin
gin_config
librosa
local_attention
matplotlib
praat-parselmouth
pyworld
PyYAML
resampy
scikit_learn
scipy
tensorboard
tqdm
wave
FreeSimpleGUI
sounddevice
gradio
noisereduce
torchfcpe

